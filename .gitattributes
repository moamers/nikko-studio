# The standalone prototype is a ~20MB single file with every image base64-inlined.
# Mark it binary so diffs/merges don't attempt to process it.
design-handoff/homepage/Nikko Homepage (standalone).html -diff -merge linguist-generated
